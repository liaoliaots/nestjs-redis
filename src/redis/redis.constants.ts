/**
 * Symbol DI token for redis module options.
 */
export const REDIS_OPTIONS = Symbol('redis options');

/**
 * Symbol DI token for redis clients.
 */
export const REDIS_CLIENTS = Symbol('redis clients');

/**
 * Symbol DI token for default redis client.
 */
export const DEFAULT_REDIS_CLIENT = Symbol('default redis client');
