export const REDIS_OPTIONS = Symbol();

export const REDIS_CLIENTS = Symbol();

export const DEFAULT_REDIS_NAMESPACE = Symbol('default-redis-client');

export const DECORATOR_DI_TOKEN_PREFIX = 'RedisClients';

export const LOGGER_CONTEXT = 'RedisModule';
